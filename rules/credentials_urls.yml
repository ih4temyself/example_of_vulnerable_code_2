rules:
- id: generic.url.credentials-in-url
  message: credentials in URL (user:pass@). use headers or requests.auth.
  severity: ERROR
  languages: [generic]
  pattern-regex: |
    (?i)[a-z][a-z0-9+.-]*://[^\s:@/]+:[^\s@/]+@